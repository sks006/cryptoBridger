{
  "name": "backend-tunnels",
  "version": "1.0.0",
  "scripts": {
    "dev": "cargo run",
    "tunnel": "npx localtunnel --port 8080 --subdomain crypto-bridger-api",
    "dev:mobile": "npx concurrently \"npm run dev\" \"npm run tunnel\""
  }
}